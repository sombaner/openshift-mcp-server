# Minimal requirements for lightweight image
# Focus on core functionality with small footprint

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Essential utilities only
requests==2.31.0

# Kubernetes client (essential for CI/CD)
kubernetes==28.1.0

# Git operations (essential for CI/CD)
gitpython==3.1.40

# Basic JSON/data handling (no heavy ML libs)
# Note: Removing torch, transformers, numpy, scikit-learn, pillow
# This saves ~2GB+ and makes build much faster
# ML inference can be added later as a separate lightweight service if needed
