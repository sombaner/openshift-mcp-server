apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  namespace: ai-mcp-openshift
  labels:
    app.kubernetes.io/name: openshift-ai-mcp-server
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  username: ""  # Set your registry username
  password: ""  # Set your registry password/token
  email: ""     # Set your registry email
  .dockerconfigjson: |
    {
      "auths": {
        "quay.io": {
          "username": "",
          "password": "",
          "email": "",
          "auth": ""
        }
      }
    }

---
apiVersion: v1
kind: Secret
metadata:
  name: webhook-secret
  namespace: ai-mcp-openshift
  labels:
    app.kubernetes.io/name: openshift-ai-mcp-server
    app.kubernetes.io/component: webhook
type: Opaque
stringData:
  secret: "your-webhook-secret-here"  # Change this to a secure random string

---
apiVersion: v1
kind: Secret
metadata:
  name: git-credentials
  namespace: ai-mcp-openshift
  labels:
    app.kubernetes.io/name: openshift-ai-mcp-server
    app.kubernetes.io/component: git
type: Opaque
stringData:
  username: ""  # Git username
  token: ""     # Git personal access token
  ssh-key: ""   # SSH private key for Git (if using SSH)
